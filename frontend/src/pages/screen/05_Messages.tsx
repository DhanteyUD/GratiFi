import { ScreenFallback } from "@/components";

function Messages() {
  return (
    <ScreenFallback
      message="Temporarily Held Together by TODO Comments."
      subText="It's not a bug, it’s a placeholder."
    />
  );
}

export default Messages;
